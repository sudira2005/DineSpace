services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      MONGO_URI: ${MONGO_URI}
      JWT_SECRET: ${JWT_SECRET}
  client:
    build: ./DineSpace/client
    ports:
      - "5173:80"
    depends_on:
      - server
